#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_js-beautify@1.13.13@js-beautify/js/bin/js-beautify.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../_js-beautify@1.13.13@js-beautify/js/bin/js-beautify.js" "$@"
  ret=$?
fi
exit $ret
